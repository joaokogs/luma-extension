{
  "name": "luma-dashboard",
  "version": "1.1.0",
  "description": "Luma Dashboard — nova aba bonita e rápida para Firefox com boards visuais para organizar links, abas e notas.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:icons": "node scripts/build-icons.js",
    "build": "npm run build:icons && tsc && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "lucide-preact": "^1.25.0",
    "preact": "^10.22.0",
    "react-colorful": "^5.8.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.3",
    "@types/node": "^26.1.1",
    "@types/webextension-polyfill": "^0.10.7",
    "rimraf": "^5.0.7",
    "sharp": "^0.35.3",
    "typescript": "^5.4.5",
    "vite": "^5.2.13",
    "webextension-polyfill": "^0.12.0"
  }
}
